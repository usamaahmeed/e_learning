import 'package:flutter/material.dart';

class IndoxScreen extends StatelessWidget {
  const IndoxScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('IndoxScreen'),
    );
  }
}
